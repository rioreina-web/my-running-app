//
//  TrendsTabView.swift  — RETIRED
//
//  The "Trends" tab was retired when the Train + Trends tabs were collapsed
//  into a single analytical **Training** tab (see training-tab-spec.md).
//  Its analytics now live on that tab; the deep "load & intensity audit"
//  it linked to still exists at Analysis/TrainingAnalysisView.swift.
//
//  `TrendsAthleteState` (its one externally-used type) was relocated to
//  Analysis/TrendsAthleteState.swift.
//
//  This file is intentionally left as a tombstone — the Xcode project uses
//  file-system synchronized groups, so removing it cleanly just means
//  deleting the file from disk when convenient.
//
