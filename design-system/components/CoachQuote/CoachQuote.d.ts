import * as React from "react";

export interface CoachQuoteProps {
  children?: React.ReactNode;
  style?: React.CSSProperties;
}

/** "From your coach" blockquote — 2px coral-at-50% stripe, italic body. */
export declare function CoachQuote(props: CoachQuoteProps): React.ReactElement;
