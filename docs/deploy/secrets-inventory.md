# Secrets inventory — 2026-06-11

Phase 5.3 of docs/ops-delivery-roadmap-2026-06-10.md. Every secret the stack
reads, where it lives, and who consumes it. Generated by grepping
`Deno.env.get` / `process.env` / `os.getenv` across the repo.

Rotation rule of thumb: anything marked **high** should be rotatable in
under an hour (you know where to mint a new one and every place to paste
it). Verify that once; rotate on any suspected exposure.

## 1. Supabase Edge Functions (Dashboard → Edge Functions → Secrets)

| Secret | Consumers | Sensitivity | Notes |
|---|---|---|---|
| `SUPABASE_URL` / `SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` | all functions | service-role: **high** | Injected by platform; service-role key bypasses RLS — never expose to clients. |
| `GEMINI_API_KEY` | 16 functions + eval harness | **high** (spend) | The main LLM key. Covered by the daily spend alert; hard cap = GCloud billing (checklist #1, still open). |
| `GROQ_API_KEY` | coaching-agent, process-training-memo | **high** (spend) | Transcription/fast-path. |
| `OPENAI_API_KEY` | process-training-memo | **high** (spend) | Whisper fallback. |
| `ALLOWED_ORIGIN` | every function via `_shared/cors.ts` | medium | **Required in prod** — new cors.ts throws on import when unset (fail-closed). Smoke test verifies. |
| `UPSTASH_REDIS_URL` / `UPSTASH_REDIS_TOKEN` | `_shared/rateLimit.ts` (all rate-limited fns) | medium | If unset, rate limiting **fails closed** (denies). Verify set in prod — open checklist item #6. |
| `UPSTASH_VECTOR_URL` / `UPSTASH_VECTOR_TOKEN` | `_shared` (vector search) | medium | |
| `STRAVA_CLIENT_ID` / `STRAVA_CLIENT_SECRET` | strava-test-pull | medium | OAuth app creds. |
| `RECONCILE_SHARED_SECRET` | reconcile-log | medium | Trigger→function shared secret. |
| `COACHING_AGENT_DUMP_PROMPT` / `DENO_ENV` | debug toggles | low | Must be UNSET in prod. |

No `ANTHROPIC_API_KEY` consumers remain (fitness-predictor was the last; deleted 2026-06-11) — if that key is still set in Supabase secrets, remove and revoke it.

## 2. Postgres Vault (`vault.decrypted_secrets` — SQL editor to manage)

| Secret | Consumers | Notes |
|---|---|---|
| `supabase_url`, `service_role_key` | pg_net trigger functions (outbox enqueuers, cron dispatchers) | Triggers no-op with RAISE WARNING when absent (local dev). |
| `slack_alerts_webhook_url` | daily LLM spend alert | The only alerting channel today. |

## 3. GitHub Actions (repo Settings → Secrets → Actions)

| Secret | Consumers | Status |
|---|---|---|
| `SUPABASE_ACCESS_TOKEN` | deploy.yml, drift-detector.yml | **to add** |
| `SUPABASE_DB_PASSWORD` | deploy.yml (db push), drift-detector.yml | **to add** |
| `SUPABASE_PROJECT_REF` | deploy.yml, drift-detector.yml | **to add** |
| `SUPABASE_URL` / `SUPABASE_ANON_KEY` | post-deploy smoke | **to add** |
| `SUPABASE_DB_URL` | ci.yml db-lint (skips when absent) | **to add** |
| `GEMINI_API_KEY` | future: eval recording in CI (Phase 2.4) | optional |

## 4. Vercel (web)

`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` (public by design); `SUPABASE_SERVICE_ROLE_KEY` (**high** — server-side only, audit which route handlers need it); `UPSTASH_REDIS_REST_URL/TOKEN`; `SANITY_API_TOKEN` + `NEXT_PUBLIC_SANITY_*`; `VITAL_API_KEY/BASE_URL/USER_ID` (verify still used — if the Vital integration is dead, remove + revoke).

## 5. Railway (ml-service)

`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` (**high**), `SUPABASE_JWT_SECRET` (**high** — verifies user JWTs), `ALLOWED_ORIGINS`, `SENTRY_DSN` (unset? — wiring exists, ties into Phase 5.1 error tracking), `ENV`/`PORT` (platform).

## Open actions

1. Verify `UPSTASH_REDIS_URL/TOKEN` and `ALLOWED_ORIGIN` set in prod (smoke test + dashboard).
2. Remove + revoke `ANTHROPIC_API_KEY` from Supabase secrets if present (no consumers).
3. Audit Vercel for stale `VITAL_*` and unused tokens; revoke stale.
4. Add the 6 GitHub Actions secrets (unblocks Deploy workflow + drift detector + db-lint).
5. `SENTRY_DSN` exists in ml-service config but error tracking is unwired across edge + web + iOS — Phase 5.1.
